/*
ID: amble_s1
LANG: C++
TASK: holstein
*/

#include <fstream>
#include <cassert>
#include <algorithm>

using namespace std;

int main() {
  ifstream infile("holstein.in");
  ofstream outfile("holstein.out");
  if (!infile || !outfile) return -1;

  return 0;
}